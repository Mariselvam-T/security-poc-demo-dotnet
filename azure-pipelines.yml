# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: windows-latest

steps:
- powershell: |
        Invoke-WebRequest -Uri https://github.com/github/codeql-action/releases/latest/download/codeql-bundle-win64.tar.gz -OutFile $(Agent.BuildDirectory)/s
        tar -xvzf $(Agent.BuildDirectory)/s/codeql-bundle-win64.tar.gz
        echo current working directory
        pwd
        echo list directory
        ls
        #./codeql/codeql resolve qlpacks
        echo Agent build directory $(Agent.BuildDirectory)
        echo Agent HomeDirectory directory $(Agent.HomeDirectory)
  displayName: 'Download Codeql CLI package'

    #Invoke-WebRequest https://github.com/github/codeql-action/releases/latest/download/codeql-bundle-linux64.tar.gz
    #tar -xvzf ./codeql-bundle-linux64.tar.gz
